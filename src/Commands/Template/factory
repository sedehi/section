<?php
/*
use Faker\Factory as Faker;
use Sedehi\Http\Controllers\{{{Classname}}}\Models\{{{Classname}}};

$factory->define({{{Classname}}}::class, function () {

    $faker = Faker::create('fa_IR');

    return [
        'name'           => $faker->name,
        'email'          => $faker->email,
        'password'       => bcrypt(str_random(10)),
        'remember_token' => str_random(10),
    ];
});

*/
